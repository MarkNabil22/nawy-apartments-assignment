import Image from 'next/image';import Link from 'next/link';import type {Apartment} from '@/lib/types';
const money=new Intl.NumberFormat('en-EG',{style:'currency',currency:'EGP',maximumFractionDigits:0});
export default function Card({apartment:a}:{apartment:Apartment}){return <article className="card"><Link href={`/apartments/${a.id}`} className="card-image"><Image src={a.imageUrl} alt={`${a.unitName} in ${a.project}`} fill sizes="(max-width: 700px) 100vw, (max-width: 1100px) 50vw, 33vw"/><span className={`status ${a.status}`}>{a.status}</span></Link><div className="card-body"><p className="eyebrow">{a.project}</p><Link href={`/apartments/${a.id}`}><h2>{a.unitName}</h2></Link><p className="location">{a.location} · Unit {a.unitNumber}</p><div className="facts"><span>{a.bedrooms} beds</span><span>{a.bathrooms} baths</span><span>{a.areaSqm} m²</span></div><strong className="price">{money.format(a.price)}</strong></div></article>}
